#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* Response(int max, int min);
void swap(int* xp, int* yp);
void printArray(int arr[], int size);
void sortarray(int arr[], int n);
void defineranges(int arr[], int n);
